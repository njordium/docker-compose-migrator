# docker-compose-migrator
Migrate Docker Compose stacks between servers via a third-party client. Handles volumes, compose files, and images with SSH multiplexing, pre-flight checks, backup/restore, per-service selection with dependency resolution, and database-aware shutdown. Runs from macOS or Linux.
